const LoadingSpinner = () => {
  return (
    <div className="loader border-4 border-black dark:border-white border-b-transparent w-12 h-12 rounded-full animate-spin"></div>
  );
};

export default LoadingSpinner;
